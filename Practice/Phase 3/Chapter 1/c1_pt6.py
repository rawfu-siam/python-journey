'''
Chapter1, topic - .gitignore — what to never push
'''
'''
Task 1 (Easy): Create a practice directory, build a blank .gitignore file, 
and add a pattern rule row to completely block a secret token file named 
.env.

Task 2 (Medium): Use a wildcard token symbol inside your .gitignore file to 
create an extension filter rule that blocks any database file ending with 
the .sqlite3 format.

Task 3 (Hard): Create a mock text file named .env.example and map out two 
fake configuration labels named CLIENT_ID and CLIENT_SECRET with blank 
placeholder string indicators to show your team how to configure their 
machines safely.
'''
# Understood! will do these tasks in real projects 